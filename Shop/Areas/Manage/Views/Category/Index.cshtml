@using Shop.Models.HomeShop;
@using Shop.ViewModels.Home;

@{
    ViewData["Title"] = "Index";
}
@model IEnumerable<Category>
<div>
    <table class="table table-dark">
        <thead>
            <tr>
                <th scope="col">İd</th>
                <th scope="col">Image</th>
                <th scope="col">Name</th>
                <th scope="col">Quantity</th>
                <th scope="col">Delete</th>
                <th scope="col">Update</th>
            </tr>
        </thead>
        <tbody class="table-group-divider">
            @foreach (var item in Model ?? new List<Category>())
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Image</td>
                    <td>@item.Name</td>
                    <td>@item.Quantity</td>
                    <td> <img src="~/Admin/images/Category/@item.ImgUrl"></td>
                    <td><a asp-area="Manage" asp-controller="Category" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Delete</a></td>
                    <td><a asp-area="Manage" asp-controller="Category" asp-action="Update" asp-route-id="@item.Id" class="btn btn-success">Update</a></td>
                </tr>
            }
        </tbody>
    </table>

</div>
